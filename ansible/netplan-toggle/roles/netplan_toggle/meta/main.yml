---
galaxy_info:
  role_name: netplan_toggle
  description: Toggle IPv4 default route between eth0/eth1 using netplan + systemd-networkd drop-in.
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: ["jammy", "noble"]
dependencies: []
